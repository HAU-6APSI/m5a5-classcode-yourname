import SightingCard from './SightingCard.jsx'

// The list of sightings, or an honest message when there are none.

export default function SightingList({ sightings }) {
  // TODO: when the array is empty, render an element with data-testid="empty"
  // saying so. An empty screen with no explanation reads as a bug.

  // TODO: otherwise render a container with data-testid="sighting-list" holding
  // one <SightingCard /> per sighting.
  //
  // Research: rendering a list with .map(), and why React wants a `key` on each
  // item (hint: the sightings already have a unique id).
  return null
}
